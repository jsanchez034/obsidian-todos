## 1. Tile math module (`window-tiling.ts`)

- [x] 1.1 Invoke Skill agent-skills:test-driven-development, then write failing Vitest tests at `apps/desktop/src/bun/lib/window-tiling.test.ts` covering `tileFrame(preset, workArea)` for every preset (`left-half`, `right-half`, `top-half`, `bottom-half`, `top-left`, `top-right`, `bottom-left`, `bottom-right`, `fill`, `vertical-split-left`, `vertical-split-right`) using the work-area `{ x: 0, y: 0, width: 1920, height: 1080 }` plus one offset case `{ x: 100, y: 50, width: 1820, height: 1030 }` to catch hard-coded zeros. Tests must fail since `window-tiling.ts` does not exist yet. (test-driven-development RED) Verify: `cd apps/desktop && bun run test` shows the new tests failing with module-not-found.
- [x] 1.2 Invoke Skill agent-skills:test-driven-development, then create `apps/desktop/src/bun/lib/window-tiling.ts` exporting a `TilePreset` type union and a pure `tileFrame(preset: TilePreset, workArea: Rect): Rect` function. Implement the minimum math to make every test from 1.1 pass. No Electrobun imports. (test-driven-development GREEN) Verify: `cd apps/desktop && bun run test` shows all `window-tiling` tests passing.

## 2. Toggle state-machine pure function

- [x] 2.1 Invoke Skill agent-skills:test-driven-development, then write failing Vitest tests at `apps/desktop/src/bun/lib/toggle-decision.test.ts` for a pure `decideToggleAction({ exists, focused, hasCurrentFile })` returning `"hide" | "focus" | "show" | "show-tray-menu"`. Cover all four scenarios from the spec: focused-popup → hide, unfocused-popup → focus, no-popup-with-file → show, no-popup-no-file → show-tray-menu. (test-driven-development RED) Verify: `cd apps/desktop && bun run test` shows the new tests failing with module-not-found.
- [x] 2.2 Invoke Skill agent-skills:test-driven-development, then create `apps/desktop/src/bun/lib/toggle-decision.ts` implementing `decideToggleAction` with the minimum branching to pass tests from 2.1. No side effects, no Electrobun imports. (test-driven-development GREEN) Verify: `cd apps/desktop && bun run test` shows all `toggle-decision` tests passing.

## 3. RPC contract for window operations

- [x] 3.1 Invoke Skill agent-skills:api-and-interface-design, then extend `apps/web/src/lib/rpc-schema.ts` with the new requests `closeWindow`, `setWindowFrame`, `setWindowFullScreen`, `restoreWindow`, and `getDisplayWorkArea` per the spec's RPC requirement. Define precise input and response types (numbers must be `number`, no `any`). Do NOT implement handlers yet. (api-and-interface-design) Verify: `bun run check-types` passes.
- [x] 3.2 Invoke Skill agent-skills:test-driven-development, then write failing Vitest tests at `apps/desktop/src/bun/lib/window-rpc-validators.test.ts` covering an `isValidFrameInput(input): boolean` validator that rejects NaN, Infinity, negative widths/heights, and non-number values, and accepts well-formed `{ x, y, width, height }`. (test-driven-development RED) Verify: `cd apps/desktop && bun run test` shows the new tests failing with module-not-found.
- [x] 3.3 Invoke Skill agent-skills:test-driven-development, then create `apps/desktop/src/bun/lib/window-rpc-validators.ts` exporting `isValidFrameInput` with the minimum logic to pass tests from 3.2. (test-driven-development GREEN) Verify: `cd apps/desktop && bun run test` shows all validator tests passing.
- [x] 3.4 Invoke Skill agent-skills:incremental-implementation, then in `apps/desktop/src/bun/index.ts` add bun-side handlers for `closeWindow`, `setWindowFrame`, `setWindowFullScreen`, `restoreWindow`, `getDisplayWorkArea` inside the existing `BrowserView.defineRPC` block. Use `isValidFrameInput` to gate `setWindowFrame`. The handlers MUST no-op gracefully when `popupWindow` is null. Do NOT yet wire `restoreWindow` into alwaysOnTop sequencing — that comes in task 7. For now, `restoreWindow` just calls `setFrame` to the anchor position computed by `calculatePopupPosition` and exits full-screen if active. (incremental-implementation) Verify: `bun run check-types` passes and `bun run dev:desktop` launches without errors.

## 4. Remove blur-auto-close + add focus tracking

- [x] 4.1 Invoke Skill agent-skills:incremental-implementation, then in `apps/desktop/src/bun/index.ts` remove the `popupWindow.on("blur", () => hidePopup())` handler at lines ~180-182. Add a module-scoped `let isPopupFocused = false` and register `popupWindow.on("focus", ...)` and `popupWindow.on("blur", ...)` listeners that maintain the boolean. Reset to `false` inside the existing `close` handler. (incremental-implementation) Verify: `bun run dev:desktop` launches; opening the popup, clicking another app, and clicking back leaves the popup intact (visually confirm — no destroy fires).

## 5. togglePopup uses the decision function

- [x] 5.1 Invoke Skill agent-skills:incremental-implementation, then refactor `togglePopup` in `apps/desktop/src/bun/index.ts` to call `decideToggleAction({ exists: !!popupWindow, focused: isPopupFocused, hasCurrentFile: !!currentFilePath })` and dispatch on the result: `"hide" → hidePopup()`, `"focus" → popupWindow.focus()`, `"show" → showPopup()`, `"show-tray-menu" → showTrayContextMenu()`. (incremental-implementation) Verify: `bun run dev:desktop` and manually exercise: hotkey while popup focused destroys it; hotkey while popup unfocused (after clicking another app) brings popup to front without destroying.

## 6. Install shadcn primitives

- [x] 6.1 Invoke Skill agent-skills:frontend-ui-engineering, then run `npx shadcn@latest add popover button -c packages/ui` to install the Popover and Button primitives into the shared UI package. Confirm both components export from `packages/ui` and are importable in `apps/web`. (frontend-ui-engineering) Verify: `bun run check-types` passes; a dummy import of both components in any web file resolves.

## 7. TitleBar component

- [x] 7.1 Invoke Skill agent-skills:frontend-ui-engineering, then create `apps/web/src/components/title-bar.tsx` exporting a `<TitleBar onZoomClick={...} />` component with a 28px-tall bar containing a red close button (left) and a green zoom button (right). The bar uses `-webkit-app-region: drag`; both buttons use `-webkit-app-region: no-drag`. Each button has an explicit `aria-label` ("Close window", "Zoom window"), keyboard activation (Enter/Space), and a visible focus ring matching the app's existing focus style. The close button calls the new `closeWindow` RPC; the zoom button invokes `onZoomClick`. (frontend-ui-engineering) Verify: `bun run check-types` passes; `bun run dev:desktop` shows the bar at the top of the popup, close button destroys the window, Tab moves focus through both buttons.

## 8. ZoomPopover component + wire tile actions

- [x] 8.1 Invoke Skill agent-skills:frontend-ui-engineering, then create `apps/web/src/components/zoom-popover.tsx` using shadcn `Popover` from `packages/ui`. Render three sections — Move & Resize (left/right/top/bottom halves), Fill & Arrange (fill, vertical-split-left, vertical-split-right, quadrants), and Full Screen + Restore actions. Each tile button shows a small SVG icon previewing the layout and has an `aria-label` describing the action. The popover anchors to the zoom button passed via `<ZoomPopover triggerRef={...} open={...} onOpenChange={...} />` (or use shadcn's standard trigger composition). (frontend-ui-engineering) Verify: `bun run check-types` passes; opening the popup and clicking the zoom button shows the popover with all sections; Escape closes it without resizing.
- [x] 8.2 Invoke Skill agent-skills:incremental-implementation, then wire each tile button's onClick to: (1) call the `getDisplayWorkArea` RPC, (2) compute the target frame via `tileFrame(preset, workArea)` imported from a small shared helper (duplicate the function into `apps/web/src/lib/window-tiling.ts` or import via tsconfig path — the function is pure and tiny), (3) call the `setWindowFrame` RPC. Wire Full Screen → `setWindowFullScreen({ value: true })`, Restore → `restoreWindow()`. Close the popover after each action. (incremental-implementation) Verify: `bun run dev:desktop`; clicking each tile preset visibly moves/resizes the popup to the expected region; Restore returns it to the anchor position.

## 9. alwaysOnTop ↔ zoom state coupling

- [x] 9.1 Invoke Skill agent-skills:test-driven-development, then in `apps/desktop/src/bun/lib/toggle-decision.test.ts` (or a new sequencing test file), add failing tests for a pure helper `alwaysOnTopSequence(targetFrame, anchorFrame): { before: "off"|"on"|null, after: "off"|"on"|null }` that returns `{ before: "off", after: null }` when the target differs from the anchor (zoom-in) and `{ before: null, after: "on" }` when the target equals the anchor (zoom-out). (test-driven-development RED) Verify: `cd apps/desktop && bun run test` shows the new tests failing.
- [x] 9.2 Invoke Skill agent-skills:test-driven-development, then implement `alwaysOnTopSequence` in `apps/desktop/src/bun/lib/toggle-decision.ts` (or a sibling module) with the minimum logic to pass tests from 9.1. (test-driven-development GREEN) Verify: `cd apps/desktop && bun run test` shows the sequencing tests passing.
- [x] 9.3 Invoke Skill agent-skills:incremental-implementation, then in `apps/desktop/src/bun/index.ts` update the `setWindowFrame`, `setWindowFullScreen`, and `restoreWindow` RPC handlers to use `alwaysOnTopSequence` to decide whether to call `setAlwaysOnTop(false)` BEFORE the frame change or `setAlwaysOnTop(true)` AFTER. The anchor frame is recomputed on each call via `calculatePopupPosition(tray.getBounds(), ...)`. Full-screen is treated as a non-anchor frame. (incremental-implementation) Verify: `bun run dev:desktop`; with another full-screen app on a different macOS Space, switching to the popup space and zooming the popup to "Fill" no longer leaves it floating above the full-screen app on return; Restore brings alwaysOnTop back.

## 10. End-to-end verification + review

- [x] 10.1 Invoke Skill agent-skills:incremental-implementation, then run the full type and test suites and manually exercise every spec scenario in `bun run dev:desktop`: titlebar renders with two controls, edge-drag does nothing, click-away keeps popup open, hotkey on focused popup destroys, hotkey on unfocused popup focuses, every tile preset works, Full Screen works, Restore returns to anchor + alwaysOnTop. (incremental-implementation) Verify: `bun run check-types` and `bun run test` both pass; the manual checklist above is fully green.
- [x] 10.2 Invoke Skill agent-skills:code-review-and-quality, then run a five-axis review (correctness, readability, architecture, security, performance) over the diff. Focus areas: (a) RPC handler input validation against the security threat surface, (b) listener leaks across hotkey hide/show cycles in `index.ts`, (c) titlebar `aria-label` and Tab order, (d) `decideToggleAction` and `tileFrame` purity (no hidden globals). File any Critical/Important findings as TODOs to address before merge. (code-review-and-quality) Verify: review notes captured in the PR description; no Critical findings remain unresolved.
- [x] 10.3 Invoke Skill agent-skills:git-workflow-and-versioning, then ensure the change is structured as atomic commits aligned with task groups (1: tile math, 2: toggle decision, 3: RPC, 4: blur removal, 5: togglePopup, 6: shadcn install, 7: titlebar, 8: zoom popover, 9: alwaysOnTop, 10: review). Conventional-commit style per project convention. (git-workflow-and-versioning) Verify: `git log --oneline` shows a clean per-group commit sequence on this branch.
